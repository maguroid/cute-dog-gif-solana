{
  "name": "gif-portal",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "yarn workspaces info",
    "web:start": "yarn --cwd packages/web start",
    "web:build": "yarn --cwd packages/web build",
    "anchor:test": "yarn --cwd packages/myepicproject test",
    "anchor:build": "yarn --cwd packages/myepicproject test",
    "anchor:deploy": "yarn --cwd packages/myepicproject test",
    "keypair:create": "yarn workspace @gif-portal/web keypair:create"
  }
}
